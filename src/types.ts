export interface NavBarProps {
  isHome?: boolean,
  isEducation?: boolean,
  isProjects?: boolean,
}

export interface ProjectProps {
  img: string,
  imgAlt: string,
  title: string,
  info: string
}